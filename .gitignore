# No binaries in git — ever. Release assets live in GitHub Releases;
# the bootstrap installs the exe under %LOCALAPPDATA%, not here.
*.exe
*.dll
*.mcpb
*.zip
*.msi
bin/
node_modules/

# Access artifacts never belong in the public dist repo
*.accdb
*.mdb
*.laccdb
*.adp

# local scratch
*.log
.DS_Store
